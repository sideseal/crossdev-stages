# Host build deps installed into every sandbox during prepare.
# Per-board extras can be added in boards/<board>/sandbox-packages.txt
# (additive — both lists are emerged).

# Bootstrap: compression tools for portage tree snapshot
app-arch/zstd
app-arch/bzip2
app-arch/xz-utils

# Cross-toolchain + portage utilities
sys-devel/crossdev
sys-devel/bc
sys-apps/merge-usr
app-eselect/eselect-repository

# Source / VCS
dev-vcs/git

# Boot / firmware
dev-embedded/u-boot-tools
sys-apps/dtc
sys-kernel/dracut
sys-apps/busybox

# Image generation
sys-fs/genimage
sys-fs/dosfstools
sys-fs/mtools

# Languages used during build. -bin: this is host tooling with no CFLAGS/USE
# tuning need, and dev-lang/rust from source needs ~9.6GB of tmpfs space at
# build time (dev-lang/rust's own check-reqs floor) that a RAM-constrained
# build host may not have.
dev-lang/rust-bin
dev-python/pyelftools
