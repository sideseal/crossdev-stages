# Until defaults/target-packages.txt is back on master (it was dropped by
# 37ccaa1), every board carries its own list.  Once the defaults land this
# shrinks to the genuine extras and the rest comes for free.

# Base userland.  grow-rootfs needs no package of its own: it resizes with
# sfdisk and partx, both from util-linux, which @system already pulls in.
net-misc/ntp
app-admin/metalog
sys-apps/busybox
app-editors/vim
sys-process/htop
app-misc/fastfetch
dev-vcs/git
app-portage/gentoolkit

# Mesa for the Mali-T620.  Midgard v4 caps at GLES 2.0 and there is no Vulkan,
# so this is panfrost only; VIDEO_CARDS is written by pre-deps.sh.
media-libs/mesa

# USB WiFi dongle (rtl8xxxu / RTL8188FU, kernel side in
# kernel-config/wifi-rtl8188fu). SSID/PSK are not baked in here -- that's a
# per-deployment secret, configured on the device after boot, not committed
# to this repo.
net-wireless/wpa_supplicant
net-wireless/iw
net-wireless/wireless-regdb
